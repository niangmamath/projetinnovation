<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>GreenFlight</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-gray-100 text-gray-800">
  <div class="container mx-auto p-4">
    <header class="bg-white shadow-md rounded-lg p-6 mb-8 flex justify-between items-center">
      <a href="/"><h1 class="text-3xl font-bold text-green-600">GreenFlight</h1></a>
      <nav>
        <!-- Leaderboard link is always visible -->
        <a href="/users/leaderboard" class="text-green-600 font-semibold hover:underline ml-4">Green Champions</a>

        <% if (userId) { %>
          <a href="/users/dashboard" class="text-blue-500 hover:underline ml-4">Dashboard</a>
          <a href="/users/logout" class="text-blue-500 hover:underline ml-4">Logout</a>
        <% } else { %>
          <a href="/users/login" class="text-blue-500 hover:underline ml-4">Login</a>
          <a href="/users/register" class="bg-blue-500 text-white font-bold py-2 px-4 rounded hover:bg-blue-700 ml-4">Register</a>
        <% } %>
      </nav>
    </header>
    <main>